import classNames from 'classnames';

/**
 * Utility function to conditionally join class names
 * Wrapper around classnames library for consistency
 */
export const cn = classNames;

export default cn;
